# 核心库（允许浮动）
face_alignment
imageio==2.34.0      # 固定有 bug 的包
imageio-ffmpeg
librosa
numba
resampy
pydub
scipy>=1.11.0
kornia
tqdm
yacs
PyYAML
joblib
scikit-image
basicsr
facexlib
gradio
gfpgan
av
safetensors
torch==2.9.1
torchvision==0.23.0  # ✅ 必须指定，不能用 0.24.1
torchaudio==2.9.1

# 只在特定平台需要
# (arm64 上 av 可能需编译)
